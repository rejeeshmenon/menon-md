---
import { cv } from '../../lib/cv';
---

<ul class="plain-list">
  {
    cv.honors.map((h) => (
      <li>
        <span>
          <strong>{h.title}</strong>
          {h.organization && <span>, {h.organization}</span>}
          {h.note && <span class="muted"> {h.note}</span>}
        </span>
        {h.year && (
          <span class="when">
            <time datetime={String(h.year)}>{h.year}</time>
          </span>
        )}
      </li>
    ))
  }
</ul>
